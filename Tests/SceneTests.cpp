#include <gtest/gtest.h>

#include "Scene.h"

/*
* @brief Verifies that a Scene object can be created successfully
* 
* This test ensures the Scene constructor works correctly
*  and that a basic Scene instance can exists without errors.
*/
TEST(SceneTests, CanCreateScene)
{
	Thrive::Scene scene; 

	EXPECT_TRUE(true);
}